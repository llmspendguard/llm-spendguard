# Docs site toolchain (pinned for reproducible builds). Not a runtime dependency of spendguard —
# the package itself has ZERO required deps. Build locally: pip install -r requirements-docs.txt && mkdocs serve
mkdocs-material==9.7.7
