{
  "_comment": "Every bare function name shared by more than one definition in src/spendguard, with the verdict a model reached by reading the BODIES (scripts/probe/review_axes.py name). PROTOCOL = intentional polymorphism, correct as-is. DUPLICATION = one job, many copies; merge. COLLISION = different jobs wearing one name; rename. This file is a BASELINE, not an approval: tests/test_names_stay_unique.py fails on any name NOT listed here, so new collisions must be judged before they land. Shrinking this file is the goal.",
  "generated_by": "scripts/probe/review_axes.py name --run",
  "names": {
    "_apply": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "Completely different jobs: gate._apply monkey-patches a class method with a gating wrapper; validate._apply computes lifecycle field updates for an insight based on a verdict. Same bare name, unrelate"
    },
    "_bin": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "Both are private module helpers resolving a CLI binary, but for DIFFERENT tools (codex vs claude) with different env pins (SPENDGUARD_CODEX_BIN vs SPENDGUARD_CLAUDE_BIN) and even divergent logic \u2014 cod"
    },
    "_capture": {
      "n": 2,
      "verdict": "PROTOCOL",
      "why": "Both are capture hooks feeding gate._record_rt from different interception layers (raw HTTP vs Vertex SDK), each with layer-specific parsing and different signatures. They share the 'capture a provide"
    },
    "_cfg": {
      "n": 3,
      "verdict": "COLLISION",
      "why": "Three unrelated jobs: bulkgate._cfg(name,default,cast) fetches a single typed setting; config._cfg() returns the whole cached config dict with mtime invalidation; emit._cfg() returns the cached 'emit'"
    },
    "_conf": {
      "n": 2,
      "verdict": "DUPLICATION",
      "why": "Identical clamping/coercion logic and even the same docstring rationale, one nested in a function and one module-level. Same job, two copies."
    },
    "_cost": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "Different signatures and different jobs. pricing._cost takes explicit token counts + flags and always applies batch/non-batch rates based on the `batch` argument. reconcile_anthropic._cost takes an An"
    },
    "_db": {
      "n": 8,
      "verdict": "COLLISION",
      "why": "Each _db() opens/manages a connection to a DIFFERENT table schema (charges, gate_ledger, call_io, calls, savings, insights) in the same SQLite file. They share the module-private accessor pattern but "
    },
    "_digest": {
      "n": 2,
      "verdict": "PROTOCOL",
      "why": "Both parse one session file into a digest dict (project/day/model/cost/prompt) but for DIFFERENT tools \u2014 Claude Code jsonl vs Codex jsonl. They implement the same contract 'session file \u2192 work-row dig"
    },
    "_enabled": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "Both are module-private feature-flag checkers but for entirely different features (chat vs http_capture), using different env vars, config keys, defaults, and truthiness parsing. They share a name onl"
    },
    "_est_line": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "advisor._est_line prints a formatted table row from six positional args (side-effect, returns None); gate._est_line takes an est dict and RETURNS a human-facing cost phrase string. Different signature"
    },
    "_fire": {
      "n": 2,
      "verdict": "DUPLICATION",
      "why": "Both bodies are byte-for-byte identical, performing the same one-shot fire-guard logic on the shared _sg tuple. The sync/async proxy distinction is irrelevant here since _fire itself does nothing asyn"
    },
    "_get": {
      "n": 3,
      "verdict": "COLLISION",
      "why": "Three different HTTP GET helpers targeting different services (Lambda API, Anthropic API, vast.ai) with different signatures, auth schemes, and base URLs. lambda_adapter._get and resources._get are ne"
    },
    "_iso_period": {
      "n": 2,
      "verdict": "DUPLICATION",
      "why": "Both are byte-identical one-line wrappers delegating to attribution.iso_period. The docstring itself admits they are the same wrapper copied into two modules; the real logic already lives in one place"
    },
    "_key": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "Both are private module-level helpers named _key that fetch API keys, but for entirely different services: one gets ANTHROPIC_API_KEY (raising RuntimeError if missing), the other gets VAST_API_KEY (fa"
    },
    "_load_state": {
      "n": 4,
      "verdict": "DUPLICATION",
      "why": "The docstring itself admits 'FOUR copies of this existed' and the shared logic (delegate to config.load_state with an adapter key and an empty shape) has already been extracted into config.load_state."
    },
    "_norm": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "One normalizes an adapter response dict (provider result shape), the other normalizes whitespace in a string. Entirely different jobs sharing a bare name."
    },
    "_one": {
      "n": 2,
      "verdict": "DUPLICATION",
      "why": "Both are local closures inside fan_out and first_ok that compute a time budget then call() with identical argument forwarding; the only difference is the throwaway variable name (_basis vs _b). Same j"
    },
    "_opt": {
      "n": 3,
      "verdict": "DUPLICATION",
      "why": "All three are local helpers that fetch the value following a flag in an argument list, differing only in the list variable name (r, rest_l, argv) and trivial index-bounds phrasing. Same job, copy-past"
    },
    "_paged": {
      "n": 2,
      "verdict": "DUPLICATION",
      "why": "Both implement identical pagination-following logic over a JSON API returning data/has_more/next_page. Differences are cosmetic: iteration cap (80 vs 60), hardcoded page_param 'page' vs parameterized,"
    },
    "_pctl": {
      "n": 2,
      "verdict": "DUPLICATION",
      "why": "Both compute a percentile of token counts. bulkgate._pctl explicitly documents that it consolidates the algorithm into calibrate._quantile and calls out that the prompts version's return-0-on-empty be"
    },
    "_persist": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "review._persist parses insights and writes them to a learning store; vendor_call._persist appends a JSON log row of a call record. Entirely different jobs sharing a generic name."
    },
    "_project_of": {
      "n": 2,
      "verdict": "DUPLICATION",
      "why": "Both are thin wrappers around config.project_of_cwd differing only by a hardcoded default tag ('claude-code' vs 'codex'). Same logic, parameterized by a constant \u2014 a textbook merge candidate. The comm"
    },
    "_register_builtins": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "Both are module-private lazy registration hooks but register entirely different registries: gpu_port registers GPU compute source adapters (register_source with gpu:<provider> keys), while sources reg"
    },
    "_resolve": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "One resolves conversation segments by conv_id with caching; the other resolves a setting's value/source from env/file/default. Entirely unrelated jobs sharing a generic name."
    },
    "_run": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "report._run(a) is a top-level report-generation entry point; vendor_call._attempt._run() is a nested closure that executes a vendor API call into a shared box. Completely different jobs, unrelated sig"
    },
    "_save_state": {
      "n": 3,
      "verdict": "PROTOCOL",
      "why": "Each is a thin per-adapter wrapper delegating to the shared config.save_state with a distinct adapter key ('chat', 'claudecode', 'codex'). The docstring documents that the actual duplication was alrea"
    },
    "_session_digests": {
      "n": 2,
      "verdict": "PROTOCOL",
      "why": "Both produce per-session digest lists filtered by days/cost, but from fundamentally different data sources: claudecode globs jsonl files on disk, codex reads a cached persisted state dict. Same contra"
    },
    "_state_path": {
      "n": 4,
      "verdict": "PROTOCOL",
      "why": "Each adapter module defines its own module-private _state_path returning config.state_path with its own adapter key ('chat', 'claudecode', 'codex', 'realized'). Same naming rule, per-adapter identity\u2014"
    },
    "_toklen": {
      "n": 3,
      "verdict": "DUPLICATION",
      "why": "All three estimate token length from a string. attribution and chat are nearly identical chars/4 heuristics (differing only in empty-string handling: 1 vs 0), and claudecode is the superior version th"
    },
    "_toks": {
      "n": 2,
      "verdict": "PROTOCOL",
      "why": "Both are provider-specific token extractors that fulfill the same conceptual contract ((in_tok, out_tok) from a response), each living in its own adapter module and taking provider-shaped arguments. T"
    },
    "_uid": {
      "n": 2,
      "verdict": "DUPLICATION",
      "why": "Both are byte-identical thin wrappers delegating to config.uid(); the same job implemented twice in two modules."
    },
    "_warn_once": {
      "n": 2,
      "verdict": "DUPLICATION",
      "why": "Both are thin module-local wrappers over config.warn_once with the same dedup intent; comments explicitly note each module kept its own copy over one shared registry. The only difference is a prefix a"
    },
    "_windows": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "receipt._windows() computes date range strings (today/7d/month), while schedule._windows(interval, remove) installs/removes a Windows Task Scheduler job. Entirely different jobs; the second is named f"
    },
    "_wrap": {
      "n": 3,
      "verdict": "COLLISION",
      "why": "All three are decorator factories wrapping an orig callable and setting w._spend_gated, but they do genuinely different jobs with different signatures: bedrock's _wrap intercepts boto3 make_api_call b"
    },
    "attribute_gap": {
      "n": 5,
      "verdict": "PROTOCOL",
      "why": "All five share the same signature (gap, since=None) and implement one 'attribute the residual gap to sources' contract. reconcile.Source is the base/interface; the others are per-source implementation"
    },
    "audit": {
      "n": 3,
      "verdict": "COLLISION",
      "why": "Three unrelated jobs share the name 'audit': cacheaudit.audit prints a cache-cost report, coverage.audit enumerates venvs with LLM SDKs, and SpendLedger.audit appends a row to a hash-chained audit log"
    },
    "available": {
      "n": 2,
      "verdict": "PROTOCOL",
      "why": "Both are lane-level availability checks conforming to a shared duck-typed lane interface; each resolves a different binary via its own _bin(), so the identical two-line body is coincidental structure,"
    },
    "build": {
      "n": 3,
      "verdict": "COLLISION",
      "why": "Three different jobs: close.build produces a month-end cost close with forecast; signal.build produces per-(project,intent,model) cost/quality signal rows; workdone.build produces per-(day,project) gi"
    },
    "call": {
      "n": 2,
      "verdict": "PROTOCOL",
      "why": "adapters.call is a higher-level facade that adds the token/input-limit guards unconditionally, and it delegates the actual vendor request to vendor_call.call (visible via bulkgate/expected_output wiri"
    },
    "captured": {
      "n": 5,
      "verdict": "PROTOCOL",
      "why": "All five are `captured(self, since=None)` methods on distinct Source-like classes, each returning a list of cost/project rows from a different backend (GPU provider, LLM gate, realtime budget, empty b"
    },
    "check": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "output_contract.check validates sample items against a data contract and returns a Result; trust.check pulls provider/ledger/server totals and returns a trust report dict. Entirely different jobs shar"
    },
    "classify": {
      "n": 3,
      "verdict": "PROTOCOL",
      "why": "These are per-source CLI subcommands (chat, claude-code, codex) each exposing a `classify` verb over their own state. claudecode and codex are near-duplicates delegating to the SHARED attribution.clas"
    },
    "cmd": {
      "n": 12,
      "verdict": "PROTOCOL",
      "why": "Each cmd(argv=None) is a subcommand entrypoint dispatched by name from a central CLI router; they implement the same conventional contract (parse argv, return exit code) for distinct subcommands. Bodi"
    },
    "configured": {
      "n": 4,
      "verdict": "PROTOCOL",
      "why": "GPUProvider defines the abstract contract (`configured(self) -> bool`) and Lambda/Modal/RunPod implement it per-provider. The two identical bodies (Lambda/RunPod) are a trivial coincidence of implemen"
    },
    "conn": {
      "n": 6,
      "verdict": "COLLISION",
      "why": "Five are property-style accessors returning a stored connection handle (self._conn) or default {} across a Source protocol family \u2014 those are legitimately polymorphic. But saas.conn() is a module-leve"
    },
    "cost_by_day": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "Different jobs: gpu_port.cost_by_day computes per-day GPU instance costs from normalized rows (returns a single {date:$} dict), while reconcile_anthropic.cost_by_day computes Anthropic API batch costs"
    },
    "count": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "callio.count returns a row count from a database table filtered by intent/model; content_tokens.count computes token counts of message content. Completely different jobs sharing a generic verb."
    },
    "day_totals": {
      "n": 3,
      "verdict": "PROTOCOL",
      "why": "Three channel adapters (claude-ai, claude-code, codex) each implement the same server contract \u2014 per-(team,project,model,day) ledger rows with identical signature and org_label routing semantics \u2014 ove"
    },
    "discover": {
      "n": 3,
      "verdict": "COLLISION",
      "why": "Three unrelated jobs share the name: chat.discover proposes an LLM taxonomy from conversations, resources.discover mines transcripts to reconstruct vast.ai instance records, sources.discover aggregate"
    },
    "emit": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "One is a local closure appending calibration cell stats into a list; the other is a module-level observability event fan-out. Completely different jobs sharing a bare name."
    },
    "estimate": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "pricing.estimate computes a dollar cost float from model/token params; _Job.estimate is a builder method that records an estimate and returns self for chaining. Different jobs, different signatures, d"
    },
    "get": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "SpendLedger.get fetches a spend event row by primary key from a ledger; semcache.get performs a cache lookup with optional semantic similarity matching. Entirely different jobs sharing a generic name."
    },
    "install": {
      "n": 6,
      "verdict": "PROTOCOL",
      "why": "The four adapter installs (bedrock, litellm, vertex) plus http_capture all follow the same idempotent, fail-open, returns-True-iff-wired contract and are dispatched uniformly by gate.install, which lo"
    },
    "instances": {
      "n": 5,
      "verdict": "PROTOCOL",
      "why": "gpu_port.GPUProvider.instances is an abstract contract (returns list, since_ts param); Lambda/Modal/RunPod adapters all implement it with the same signature and shared doctrine (NEVER raises, [] on fa"
    },
    "load": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "provider_plugins.load() is a plugin-discovery activator returning a status dict; _Boom.load() is a mock/fixture method mimicking an entry-point's .load() interface (returns a callable that raises). Th"
    },
    "load_cls": {
      "n": 2,
      "verdict": "PROTOCOL",
      "why": "Both accessors read their own module's state file (via each module's own _load_state), exposing per-session classifications with a deliberately shared contract. The codex docstring explicitly names 'p"
    },
    "main": {
      "n": 39,
      "verdict": "PROTOCOL",
      "why": "Each `main` is a distinct CLI subcommand entrypoint parsing its own args and dispatching to its module's work. They share the conventional `main(argv=None)` contract deliberately, each in a separate m"
    },
    "project_of": {
      "n": 2,
      "verdict": "PROTOCOL",
      "why": "resources.project_of is a thin convenience wrapper that delegates to gpu_port.project_of, supplying a default label_map. Same semantics by design, not a copied implementation\u2014the core matching logic l"
    },
    "push": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "Both are named `push` and both POST to the SaaS backend, but they push fundamentally different payloads to different endpoints: signal.push sends scrubbed project signal rows to /v1/signal, while trut"
    },
    "ratios": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "Both compute lists of ratios from _calls_rows but measure entirely different things: _opi_obs returns output/input token ratios, while _residual_obs returns actual cost / expected pricing-model cost r"
    },
    "record": {
      "n": 4,
      "verdict": "COLLISION",
      "why": "Four functions named `record` doing four different jobs: budget.record writes a spend charge with forensic intent/actor into a budget system; callio.record inserts a call I/O sample (prompt/output cap"
    },
    "record_estimate": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "bulkgate.record_estimate upserts a worst-case zero-spend estimate into gate_ledger keyed by call-class sig; calibrate.record_estimate logs a caller's per-job cost prediction into c"
    },
    "register": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "Two different registries: gate.register adds SDK gating hooks to a list _EXTRA with a 5-tuple signature, while sources.register maps a name to a factory in a _SOURCES dict. Differe"
    },
    "render": {
      "n": 2,
      "verdict": "PROTOCOL",
      "why": "Both are per-command view functions taking that command's own data shape and returning a formatted text report; scan.render(data) renders scan results, sources.render(d) renders sources discovery. The"
    },
    "rollup": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "Both named 'rollup' but do entirely different jobs: SpendLedger.rollup aggregates SQL spend_events into micro/USD sums with group_by/where filters; workdone.rollup groups per-day commit/intent records"
    },
    "run": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "One is a batch gate-check submission method on a _Job class that validates bulk operations before submitting; the other is a reconciliation loop over a source adapter returning cost attribution data. "
    },
    "run_prompt": {
      "n": 2,
      "verdict": "PROTOCOL",
      "why": "Two lane executors (Codex/ChatGPT plan vs Claude subscription) implementing the same headless plan-billed contract: identical signature and identical {text, in_tok, out_tok, latency, error} return sha"
    },
    "scrub": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "One redacts/truncates a label string for de-identification; the other transforms an insight dict into a shareable generalized rule. Completely different jobs, inputs, and outputs sharing the name 'scr"
    },
    "show": {
      "n": 4,
      "verdict": "PROTOCOL",
      "why": "chat, claudecode, and codex each implement a per-module `show` report command for their respective data source (claude.ai chats, claude-code sessions, codex sessions), all following the same contract:"
    },
    "snapshot": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "budget.snapshot copies the ledger DB file aside as a backup before mutation; resources.snapshot records live vast.ai instance state to a history JSON. Different jobs, different return types (path/None"
    },
    "source": {
      "n": 3,
      "verdict": "PROTOCOL",
      "why": "Three adapter modules each expose a `source()` factory that the gpu_port registry discovers by name \u2014 this is a plugin/entry-point contract where each adapter binds its own module-level PROVIDER. The "
    },
    "status": {
      "n": 3,
      "verdict": "COLLISION",
      "why": "Three unrelated jobs: bulkgate.status queries a gate ledger for a signature's freshness/test state and returns a dict; lanes.status reports per-lane CLI/auth readiness; saas.status prints SaaS client "
    },
    "story": {
      "n": 2,
      "verdict": "DUPLICATION",
      "why": "Both are 'story' commands with identical signatures (by, days, run) doing the same job: caged LLM synth over a period's data \u2192 narrative STORY + private WORK INSIGHTS, estimate-first, same _STORY_SYS/"
    },
    "summary": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "One queries a SQL DB for per-intent call cost aggregates and returns rows; the other formats a parse-result status string. Entirely different jobs sharing a bare name."
    },
    "summary_lines": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "Both return list[str] of display lines, but they summarize completely different domains: calibrate reads token-fill calibration stats from the DB, while lanes reports subscription lane executor status"
    },
    "sync": {
      "n": 8,
      "verdict": "PROTOCOL",
      "why": "The four channel-adapter syncs (chat, claudecode, codex, resources) all implement one duck-typed contract: push this channel's spend to the server, honoring visibility/contributor, org-routed, with a "
    },
    "test": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "One is a fluent builder method on _Job that configures and dispatches a batch test job (contract/items setup); the other is a standalone CLI diagnostic that checks cookie decryption and API connectivi"
    },
    "truth_total": {
      "n": 5,
      "verdict": "PROTOCOL",
      "why": "Each is a Source implementation providing the ground-truth external total for a distinct source type (GPU, LLM, realtime, base). The `since` signature and the None=UNKNOWN / 0.0=no-contribution conven"
    },
    "update": {
      "n": 4,
      "verdict": "PROTOCOL",
      "why": "chat/claudecode/codex.update all implement the same 'incremental watermark sync' contract across different data sources (Claude chat API, Claude Code transcripts, Codex sessions) \u2014 same job, different"
    },
    "verify": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "pricing.verify() self-checks hardcoded price constants and takes no arguments; remote.verify(ssh, ...) runs a remote doctor command over SSH and returns (ok, detail). Entirely different jobs, signatur"
    },
    "w": {
      "n": 12,
      "verdict": "PROTOCOL",
      "why": "Each `w` is a local closure factory-produced wrapper returned by a distinct decorator (`_wrap`, `_wrap_rt`, `_wrap_rt_units`, `_wrap` in gate) implementing the wrapping contract via functools.wraps. T"
    },
    "work": {
      "n": 2,
      "verdict": "PROTOCOL",
      "why": "Both implement the same 'WORK DONE report bucketed by period' contract for different data sources (claude.ai chat vs Claude Code sessions). They share the same signature (by, days), same output struct"
    },
    "add": {
      "n": 2,
      "verdict": "COLLISION",
      "why": "Two unrelated local helper closures: one aggregates GPU cost/hours by (project,gpu,day), the other appends token-cap findings with file/line metadata. Different signatures, different jobs, coincidenta"
    }
  }
}